experiment:
  name: resnet18_finetune_1_seed42
  description: Full fine-tuning on ResNet-18 using 1% of the training data.
  seed: 42
data:
  dataset: OxfordIIITPet
  train_split: train_1_seed42
  val_split: val_seed42
  test_split: official_test
  image_size: 224
  use_augmentation: true
  use_oversampling: false
  use_weighted_loss: false
model:
  architecture: resnet18
  pretrained: true
  num_classes: 37
  strategy: full_finetuning
  freeze_backbone: false
  unfrozen_layers: all
training:
  epochs: 30
  batch_size: 64
  optimizer: adamw
  learning_rate: 0.0001
  weight_decay: 0.0001
evaluation:
  metrics:
  - top1_accuracy
  - macro_f1
  - per_class_accuracy
  - per_class_f1
logging:
  output_dir: experiments/resnet18_finetune_1_seed42
  save_best_checkpoint: true
  save_training_curves: true
